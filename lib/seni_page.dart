import 'package:flutter/material.dart';

class SeniPage extends StatelessWidget {
  const SeniPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Seni')),
      body: const Center(
        child: Text('Halaman Seni'),
      ),
    );
  }
}
