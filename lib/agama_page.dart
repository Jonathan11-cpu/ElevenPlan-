import 'package:flutter/material.dart';

class AgamaPage extends StatelessWidget {
  const AgamaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Agama')),
      body: const Center(
        child: Text('Halaman Agama'),
      ),
    );
  }
}
