import 'package:flutter/material.dart';

class IpsPage extends StatelessWidget {
  const IpsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('IPS')),
      body: const Center(
        child: Text('Halaman IPS'),
      ),
    );
  }
}
