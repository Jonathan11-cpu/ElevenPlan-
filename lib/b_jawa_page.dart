import 'package:flutter/material.dart';

class BJawaPage extends StatelessWidget {
  const BJawaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bahasa Jawa')),
      body: const Center(
        child: Text('Halaman Bahasa Jawa'),
      ),
    );
  }
}
